# Local dev only — points auth-client directly at the local backend.
# In production (Vercel), this var is NOT set, so auth-client uses relative
# URLs which are proxied through Vercel's /api/* rewrite → same-origin cookies.
VITE_API_URL=http://localhost:3000
